plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.kotlin.compose)
}

android {
    namespace = 'krausoft.volcanesdecostarica'
    compileSdk = 36

    defaultConfig {
        applicationId = 'krausoft.volcanesdecostarica'
        minSdk = 26
        targetSdk = 36
        versionCode = 18
        versionName = '3.0'

        testInstrumentationRunner = 'androidx.test.runner.AndroidJUnitRunner'
    }

    buildTypes {
        release {
            // Sin ofuscación por ahora; se evaluará al preparar el release final.
            minifyEnabled = false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    buildFeatures {
        compose = true
    }
}

// Compila Kotlin apuntando a bytecode de Java 17 (sin descargar toolchain aparte).
kotlin {
    compilerOptions {
        jvmTarget = org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_17
    }
}

dependencies {
    implementation libs.androidx.core.ktx

    // Compose (versiones gestionadas por la BOM)
    implementation platform(libs.androidx.compose.bom)
    implementation libs.androidx.activity.compose
    implementation libs.androidx.compose.ui
    implementation libs.androidx.compose.ui.graphics
    implementation libs.androidx.compose.ui.tooling.preview
    implementation libs.androidx.compose.material3

    // Arquitectura: ViewModel + estado consciente del ciclo de vida + navegación
    implementation libs.androidx.lifecycle.viewmodel.compose
    implementation libs.androidx.lifecycle.runtime.compose
    implementation libs.androidx.navigation.compose

    // Preferencias modernas (reemplaza SharedPreferences)
    implementation libs.androidx.datastore.preferences

    // Carga de imágenes (en Coil 3 la red es un artefacto aparte)
    implementation libs.coil.compose
    implementation libs.coil.network.okhttp

    // Tests
    testImplementation libs.junit
    androidTestImplementation platform(libs.androidx.compose.bom)
    androidTestImplementation libs.androidx.junit
    androidTestImplementation libs.androidx.espresso.core
    androidTestImplementation libs.androidx.compose.ui.test.junit4

    // Herramientas de depuración/preview de Compose
    debugImplementation libs.androidx.compose.ui.tooling
    debugImplementation libs.androidx.compose.ui.test.manifest
}
